Para rodar este programa é necessário:
- Descompactar o arquivos.rar no mesmo diretório em que se encontra o main.py
- Instalar as bibliotecas:
	pip install spacy
	pip install re
	pip install pypdf2
- Executar normalmente o arquivo main.py pela linha de comando utilizando:
	python main.py